get_token:
  pattern: /api/tokens
  defaults: { _controller: TreeHouseKeystoneBundle:Token:create }
  requirements:
    _method: POST

test_api:
  pattern: /api/test
  defaults: { _controller: TreeHouseKeystoneIntegrationBundle:Api:test }
